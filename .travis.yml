sudo: false
language: python
python:
    - "2.7"
install:
    - pip install --upgrade pip
    - pip install --upgrade setuptools
    - pip install -r requirements.txt
script:
    - python -m unittest discover -s pi_time
